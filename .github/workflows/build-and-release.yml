name: Server CI/CD
run-name: Build and run latest image on server
on: [push]
jobs:
  Build-and-Run:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Gathering source code
        uses: actions/checkout@v6
      - name: "[DOCKER] Setup metadata"
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/makarim22/koda-b6-backend-nodejs
          tags: type=raw,value=latest
      - name: "[DOCKER] Login GHCR"
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner}}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: "[DOCKER] Set up QEMU"
        uses: docker/setup-qemu-action@v3

      - name: "[DOCKER] Set up Docker Buildx"
        uses: docker/setup-buildx-action@v3

      - name: "[DOCKER] Build and push"
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      - name: "[SERVER] Deploy latest image"
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
            cd server-jaka
            docker compose pull
            docker run --rm --network=server-jaka_webapps migrate/migrate:latest -source github://makarim22:${{ secrets.GITHUB_TOKEN }}@makarim22/koda-b6-backend/db/migrations -database postgresql://postgres:password@pg-db:5432/coffeeshop?sslmode=disable up
            docker compose up backend -d